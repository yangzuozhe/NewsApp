<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="example.com.newsapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mipmapicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mipmapicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".ViewPage1_shouYe_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".jinritoutiao.NewsContent"></activity>
        <activity android:name=".search_news.Search_New_Item" />
        <activity android:name=".SQL.ShouCang_NewsItem"></activity>
    </application>

</manifest>